from tkinter import *
EventLog = []


allItems = {
    atype : String


}




class window():
    def __init__ (self, x, y, others):
        self.xWid = x
        self.yWid = y
    

    def display():
        pass

class elements():
    def __init__(self) -> None:
        pass

    def add_button(self, posX, psoY, lbl):
        pass

    def press(self)
        pass
        if preseed:
          
          return True
        else:
            return False
    def 



        










"""
class Placer:
    '''
    take arguments for .place() upon
    instantiation so that they don't
    need to be passed to .place() later.
    '''
    def __init__(
            self,
            element: tk.Button,
            x: int = 0,
            y: int = 0,
        ):
        self.element = element
        self.x = x
        self.y = y
    def place(self):
        '''
        Place the widget on the window.
        '''
        self.element.place(
            x = self.x,
            y = self.y,
        )

def create_button(
        x: int = 0,
        y: int = 0,
        text: str = 'TEXT',
        width: int = 5,
        height: int = 1,
    ):
    '''
    Create a `tkinter.Button` with
    the specified options.
    '''
    button = tk.Button(
        text = text,
        width = width,
        height = height,
    )
    return Placer(button, x=x, y=y)

## processes to be assigned 1 thread each





root = tk.Tk()
root.title("parallel processing engine")

button = create_button()
button.place()
root.mainloop()
"""

def Hello_world(instr):
    if instr == "print":
        print("hello world")










































Hello_world("print")